ener('mouseleave', () => { if (cursor) cursor.style.opacity = '1'; if (follower) { follower.style.transform = `translate(calc(${followerX}px - 50%), calc(${followerY}px - 50%)) scale(1)`; follower.style.backgroundColor = 'transparent'; } }); }); } // India SVG Map Fetch & Initialization fetch('india.svg') .then(res => res.text()) .then(svgText => { const container = document.getElementById('india-map-container'); if(!container) return; // Extract just the SVG part, removing XML declarations to ensure clean injection const svgMatch = svgText.match(/<svg[\s\S]*?<\/svg>/); if (svgMatch) { container.innerHTML = svgMatch[0]; } else { container.innerHTML = svgText; } const svgElement = container.querySelector('svg'); if(svgElement) { svgElement.setAttribute('viewBox', '0 0 800 1000'); // Ensure proper scaling if needed svgElement.style.width = '100%'; svgElement.style.height = '100%'; } const paths = container.querySelectorAll('svg path'); const tooltip = document.getElementById('map-tooltip'); const tooltipState = document.getElementById('map-tooltip-state'); paths.forEach(path => { path.addEventListener('mouseenter', (e) => { const stateName = path.getAttribute('title') || 'State'; tooltipState.textContent = stateName; tooltip.style.opacity = '1'; if (cursor) cursor.style.opacity = '0'; if (follower) { follower.style.backgroundColor = 'rgba(184, 134, 11, 0.1)'; follower.style.width = '60px'; follower.style.height = '60px'; } }); path.addEventListener('mousemove', (e) => { tooltip.style.left = e.clientX + 'px'; tooltip.style.top = e.clientY + 'px'; }); path.addEventListener('mouseleave', () => { tooltip.style.opacity = '0'; if (cursor) cursor.style.opacity = '1'; if (follower) { follower.style.backgroundColor = 'transparent'; follower.style.width = '40px'; follower.style.height = '40px'; } }); path.addEventListener('click', () => { window.location.href = 'states.html'; }); }); }) .catch(err => console.error('Error loading India Map SVG:', err)); </script>
</body>
</html>